// The accordion makeup is created from WordPress hooks and transformed into the desired custom element by the 11ty build. The desired markup contains this one new class which we need not to be purged

export default function template() {
  return /*html*/`
    <div class="plus-munus"
  `
}